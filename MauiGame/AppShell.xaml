<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiGame.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiGame"
    Shell.FlyoutBehavior="Flyout"
    xmlns:views="clr-namespace:MauiGame.Views"
    Title="MauiGame">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate views:MainView}"
        Route="MainPage" />

    <FlyoutItem>
        <ShellContent ContentTemplate="{DataTemplate views:TestView}" Route="TestView" />
        <ShellContent ContentTemplate="{DataTemplate views:MainView}" Route="MainView" />
    </FlyoutItem>

</Shell>
